import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Operational Scenarios | Adigator IQ",
  description:
    "Real-world campaign validation workflows for setup, creative swaps, landing updates, renewals, outsourcing, and enterprise handoffs.",
};

export default function OperationalScenariosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
