# TODO - Meta Ads goal-specific analyzer logic

## Completed
- [x] Meta goal profiles + deterministic scoring in `app/lib/analyzers/meta.ts`
- [x] Wired into `app/api/analyze-creative/route.ts` (prompt, feed intel, API response)
- [x] `meta_ads_dynamic_eval` exposed in `ai_analysis` for AnalysisPanel UI

